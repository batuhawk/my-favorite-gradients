import { createStore } from 'vuex';
import { gradients } from './modules/gradients';

const state = {};

const getters = {};

const mutations = {};

const actions = {};

const modules = {
  gradients,
};

export default createStore({
  state,
  getters,
  mutations,
  actions,
  modules,
});
